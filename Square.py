class Rectangle:


    def __init__ (self, length, width):
        self.length = length
        self.width = width
    def areaRectangle(self):
        return self.length * self.width

    @staticmethod
    def allPossible():
        list1 = []
        if area == v * w:
            list1.append(v and w)
        return list1


rectangle = Rectangle(15,55)
print rectangle.areaRectangle()
AreaRec = rectangle.areaRectangle()
print AreaRec.allPossible()
